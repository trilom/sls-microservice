{
  "name": "ExampleMicroService",
  "description": "",
  "version": "0.1.0",
  "scripts": {
    "build": "make buildAll",
    "deploy": "make deployAll STAGE='dev'",
    "start": "make buildAll && make deployAll STAGE='dev'"
  },
  "devDependencies": {
    "serverless": "^1.41.1",
    "serverless-iam-roles-per-function": "^2.0.0",
    "serverless-plugin-reducer": "^3.2.1",
    "serverless-pseudo-parameters": "^2.4.0"
  }
}
